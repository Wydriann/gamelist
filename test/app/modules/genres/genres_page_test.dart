import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:bloc_pattern/bloc_pattern_test.dart';

import 'package:gamelist/app/modules/genres/genres_page.dart';

main() {
  testWidgets('GenresPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(GenresPage(title: 'Genres')));
    //  final titleFinder = find.text('Genres');
    //  expect(titleFinder, findsOneWidget);
  });
}
